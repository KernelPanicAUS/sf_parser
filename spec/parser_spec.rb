#!/usr/bin/env ruby
# encoding: UTF-8

require_relative './spec_helper.rb'
class Parser
  describe "Script" do
    it "should detect that the date is within the specified date range" do
    end

    it "should not count dates that are outside the specified date range" do
    end

    it "should not count a date in more than one date range" do
    end
  end
end


